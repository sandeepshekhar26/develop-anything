# Generated by auk — AI Context Engineering Platform
# https://github.com/sandeepshekhar26/develop-anything
# Generated: 2026-06-11T13:06:24.840Z
# Context Health: 100%
# DO NOT EDIT MANUALLY — regenerate with: auk compile

# Project: auk

**Languages:** typescript, javascript
**Framework:** Express
**Rules:** 22 | **Health:** 100%

---

## ⚠️ Critical Rules (ALWAYS follow these)

- **architecture-no-utility-to-model**: Utility layer should not import from Model layer. 2 existing violations are known tech debt — don't add more.
- **naming-functions-camelcase**: All function names use camelCase naming convention.
- **naming-classes-pascalcase**: All class and interface names use PascalCase naming convention.
- **testing-naming-test**: Test files follow the *.test.ts / *.test.js naming convention.

---

## Architecture

### 🔴 architecture-no-utility-to-model

Utility layer should not import from Model layer. 2 existing violations are known tech debt — don't add more.

### 🟡 architecture-hotspot-tests-helpers-expect-ts-expect

expect (tests/helpers/expect.ts) is called from 105 places — changes here have a high blast radius. Keep its signature and behavior stable.

**Evidence:**
- `tests/helpers/expect.ts:105` — fan-in 105

### 🟡 architecture-hotspot-src-utils-logger-ts-success

success (src/utils/logger.ts) is called from 21 places — changes here have a high blast radius. Keep its signature and behavior stable.

**Evidence:**
- `src/utils/logger.ts:69` — fan-in 21

### 🟡 architecture-hotspot-src-utils-logger-ts-debug

debug (src/utils/logger.ts) is called from 19 places — changes here have a high blast radius. Keep its signature and behavior stable.

**Evidence:**
- `src/utils/logger.ts:84` — fan-in 19

### 🟡 architecture-god-class-src-utils-cli-ts-Command

Command (src/utils/cli.ts) has 18 methods and 24 inbound calls — it is becoming a god object. Don't add more responsibilities; split it instead.

**Evidence:**
- `src/utils/cli.ts:46` — 18 methods, 24 inbound calls

### 🟡 architecture-god-class-tests-helpers-expect-ts-Expectation

Expectation (tests/helpers/expect.ts) has 15 methods and 2 inbound calls — it is becoming a god object. Don't add more responsibilities; split it instead.

**Evidence:**
- `tests/helpers/expect.ts:8` — 15 methods, 2 inbound calls

### 🟡 architecture-hub-src-types-rules-ts

src/types/rules.ts has 31 connections — it may be becoming a god object. Consider breaking it into smaller modules.

**Evidence:**
- `src/types/rules.ts` — 31 connections (degree centrality)

### 🟡 architecture-hub-src-utils-logger-ts

src/utils/logger.ts has 31 connections — it may be becoming a god object. Consider breaking it into smaller modules.

**Evidence:**
- `src/utils/logger.ts` — 31 connections (degree centrality)

### 🟡 architecture-hub-src-types-analysis-ts

src/types/analysis.ts has 28 connections — it may be becoming a god object. Consider breaking it into smaller modules.

**Evidence:**
- `src/types/analysis.ts` — 28 connections (degree centrality)

## Imports

### 🟡 exports-named-only

The codebase uses named exports exclusively. Avoid default exports.

**Evidence:**
- `scripts/ts-loader.mjs` — Example of this pattern
- `src/analyzer/call-graph.ts` — Example of this pattern
- `src/analyzer/import-graph.ts` — Example of this pattern

### 🟡 imports-relative

The codebase uses relative imports (e.g., ../services/user) rather than absolute/alias paths.

**Evidence:**
- `various` — Example of this pattern
- `various` — Example of this pattern
- `various` — Example of this pattern

## Types

### 🟡 types-prefer-interface

The codebase prefers interface declarations over type aliases for object shapes.

**Evidence:**
- `various` — Example of this pattern
- `various` — Example of this pattern
- `various` — Example of this pattern

## Naming

### 🔴 naming-functions-camelcase

All function names use camelCase naming convention.

**Evidence:**
- `scripts/ts-loader.mjs:7` — Example of this pattern
- `src/analyzer/call-graph.ts:26` — Example of this pattern
- `src/analyzer/call-graph.ts:119` — Example of this pattern

### 🔴 naming-classes-pascalcase

All class and interface names use PascalCase naming convention.

**Evidence:**
- `src/analyzer/call-graph.ts:21` — Example of this pattern
- `src/analyzer/parser-interface.ts:9` — Example of this pattern
- `src/analyzer/parser-interface.ts:15` — Example of this pattern

## Testing

### 🔴 testing-naming-test

Test files follow the *.test.ts / *.test.js naming convention.

**Evidence:**
- `tests/analyzer/call-graph.test.ts` — Example of this pattern
- `tests/analyzer/parser.test.ts` — Example of this pattern
- `tests/analyzer/scanner.test.ts` — Example of this pattern

## Patterns

### ℹ️ patterns-function-length

90% of functions in this codebase are 47 lines or shorter. Keep new functions in that range; extract helpers instead of growing bodies.

### ℹ️ cluster-target-compiler

12 files share the 'target-compiler' shape (key terms: target, compiler, rules). They share imports: ../../types/decisions.js, ../../types/rules.js, ../format.js. New files of this kind should follow the same structure.

**Evidence:**
- `src/commands/compile.ts` — Example of this pattern
- `src/compiler/compiler-engine.ts` — Example of this pattern
- `src/compiler/format.ts` — Example of this pattern

### ℹ️ cluster-enhancement-enhancements

5 files share the 'enhancement-enhancements' shape (key terms: enhancement, enhancements, generator). They share imports: ../types/rules.js, path. New files of this kind should follow the same structure.

**Evidence:**
- `src/commands/enhance.ts` — Example of this pattern
- `src/commands/generate.ts` — Example of this pattern
- `src/generator/enhancement-validator.ts` — Example of this pattern

### ℹ️ cluster-url-mjs

4 files share the 'url-mjs' shape (key terms: url, mjs, scripts). They share imports: fs, node:url, path. New files of this kind should follow the same structure.

**Evidence:**
- `scripts/copy-assets.mjs` — Example of this pattern
- `scripts/ts-loader.mjs` — Example of this pattern
- `scripts/ts-run.mjs` — Example of this pattern

### ℹ️ cluster-config-auk

4 files share the 'config-auk' shape (key terms: config, auk, dir). They share imports: ../types/config.js, ../utils/cli.js, ../utils/config.js. New files of this kind should follow the same structure.

**Evidence:**
- `src/analyzer/scanner.ts` — Example of this pattern
- `src/commands/badge.ts` — Example of this pattern
- `src/commands/init.ts` — Example of this pattern

### ℹ️ cluster-claims-verifier

4 files share the 'claims-verifier' shape (key terms: claims, verifier, claim). They share imports: ../types/rules.js, ../utils/logger.js. New files of this kind should follow the same structure.

**Evidence:**
- `src/commands/verify.ts` — Example of this pattern
- `src/verifier/claim-extractor.ts` — Example of this pattern
- `src/verifier/claim-verifier.ts` — Example of this pattern

### ℹ️ cluster-tfidf-semantic

4 files share the 'tfidf-semantic' shape (key terms: tfidf, semantic, cluster). They share imports: ./tfidf.js. New files of this kind should follow the same structure.

**Evidence:**
- `src/semantic/clusterer.ts` — Example of this pattern
- `src/semantic/similarity.ts` — Example of this pattern
- `src/semantic/tfidf.ts` — Example of this pattern
